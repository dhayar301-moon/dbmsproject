<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Smart Process Scheduler</title>

<!-- TITLE FONT -->
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;800&family=Syne:wght@400;500;600;700;800&display=swap" rel="stylesheet">

<style>
:root{
  --bg:#140f1f;
  --bg2:#1b1430;
  --surface:#241a3d;
  --surface2:#2d2250;

  --border:#4b3a73;
  --border2:#6a52a3;

  --accent:#d8b4fe;
  --accent2:#c084fc;
  --accent3:#fde68a;

  --text:#fff7ed;
  --text2:#e9d5ff;
  --text3:#b794f4;

  --glow:0 0 30px rgba(192,132,252,.35);
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:'Syne',sans-serif;
}

body{
  background:linear-gradient(135deg,var(--bg),var(--bg2));
  color:var(--text);
  min-height:100vh;
  overflow-x:hidden;
}

/* BACKGROUND EFFECT */
body::before{
  content:'';
  position:fixed;
  inset:0;
  background:
  linear-gradient(rgba(255,255,255,.03) 1px,transparent 1px),
  linear-gradient(90deg,rgba(255,255,255,.03) 1px,transparent 1px);
  background-size:60px 60px;
  pointer-events:none;
}

/* HEADER */
header{
  width:100%;
  height:80px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 40px;
  background:rgba(20,15,31,.92);
  border-bottom:1px solid var(--border);
  backdrop-filter:blur(20px);
  position:sticky;
  top:0;
}

.logo{
  display:flex;
  align-items:center;
  gap:16px;
}

.logo-box{
  width:50px;
  height:50px;
  border-radius:14px;
  background:linear-gradient(135deg,var(--accent2),var(--accent3));
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:24px;
  font-weight:bold;
  color:#000;
  box-shadow:var(--glow);
}

.logo-title{
  font-family:'Cinzel',serif;
  font-size:24px;
  font-weight:800;
  letter-spacing:2px;
  color:var(--accent3);
  text-shadow:0 0 15px rgba(253,230,138,.3);
}

.logo-sub{
  font-size:10px;
  letter-spacing:4px;
  color:var(--text3);
  margin-top:4px;
}

/* BUTTON */
.theme-btn{
  padding:12px 20px;
  border:none;
  border-radius:12px;
  background:linear-gradient(135deg,var(--accent2),var(--accent));
  color:#000;
  font-weight:800;
  cursor:pointer;
  transition:.3s;
  box-shadow:var(--glow);
}

.theme-btn:hover{
  transform:translateY(-2px) scale(1.03);
}

/* MAIN */
main{
  width:100%;
  max-width:1200px;
  margin:auto;
  padding:40px 20px;
}

/* CARD */
.card{
  background:var(--surface);
  border:1px solid var(--border);
  border-radius:22px;
  padding:30px;
  margin-bottom:25px;
  box-shadow:0 10px 40px rgba(0,0,0,.25);
}

/* TITLE */
.section-title{
  font-size:20px;
  font-weight:800;
  margin-bottom:20px;
  color:var(--accent3);
}

/* INPUTS */
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:20px;
}

.input-box label{
  display:block;
  margin-bottom:8px;
  font-size:12px;
  color:var(--text3);
  letter-spacing:1px;
}

.input-box input,
.input-box select{
  width:100%;
  height:48px;
  border-radius:12px;
  border:1px solid var(--border2);
  background:var(--surface2);
  color:var(--text);
  padding:0 14px;
  outline:none;
  font-size:14px;
}

.input-box input:focus,
.input-box select:focus{
  border-color:var(--accent);
  box-shadow:0 0 15px rgba(216,180,254,.25);
}

/* BUTTONS */
.actions{
  display:flex;
  gap:15px;
  margin-top:25px;
}

.btn{
  flex:1;
  height:50px;
  border:none;
  border-radius:14px;
  font-weight:800;
  cursor:pointer;
  transition:.3s;
}

.btn-primary{
  background:linear-gradient(135deg,var(--accent2),var(--accent3));
  color:#000;
}

.btn-secondary{
  background:var(--surface2);
  color:var(--text);
  border:1px solid var(--border2);
}

.btn:hover{
  transform:translateY(-2px);
}

/* STATS */
.stats{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:20px;
}

.stat-card{
  background:var(--surface2);
  border:1px solid var(--border);
  border-radius:18px;
  padding:25px;
  text-align:center;
}

.stat-title{
  font-size:12px;
  color:var(--text3);
  letter-spacing:2px;
  margin-bottom:10px;
}

.stat-value{
  font-size:38px;
  font-weight:800;
  color:var(--accent3);
}

/* TABLE */
table{
  width:100%;
  border-collapse:collapse;
}

th{
  background:rgba(255,255,255,.04);
  color:var(--accent3);
  padding:15px;
  font-size:12px;
  text-transform:uppercase;
  letter-spacing:1px;
}

td{
  padding:15px;
  border-top:1px solid var(--border);
  color:var(--text2);
  text-align:center;
}

tr:hover{
  background:rgba(255,255,255,.03);
}

/* FOOTER */
footer{
  text-align:center;
  padding:30px;
  color:var(--text3);
  font-size:12px;
  letter-spacing:3px;
}
</style>
</head>

<body>

<header>
  <div class="logo">
    <div class="logo-box">⚡</div>

    <div>
      <div class="logo-title">SMART PROCESS SCHEDULER</div>
      <div class="logo-sub">ML · OS · DBMS · DAA</div>
    </div>
  </div>

  <button class="theme-btn">Yellow Purple Theme</button>
</header>

<main>

  <!-- ADD PROCESS -->
  <div class="card">
    <div class="section-title">Add Process</div>

    <div class="grid">
      <div class="input-box">
        <label>Process ID</label>
        <input type="text" placeholder="P1">
      </div>

      <div class="input-box">
        <label>Burst Time</label>
        <input type="number" placeholder="5">
      </div>

      <div class="input-box">
        <label>Arrival Time</label>
        <input type="number" placeholder="0">
      </div>

      <div class="input-box">
        <label>Priority</label>
        <input type="number" placeholder="1">
      </div>

      <div class="input-box">
        <label>Type</label>
        <select>
          <option>CPU Bound</option>
          <option>IO Bound</option>
        </select>
      </div>
    </div>

    <div class="actions">
      <button class="btn btn-primary">Add Process</button>
      <button class="btn btn-secondary">Clear</button>
    </div>
  </div>

  <!-- STATS -->
  <div class="stats">

    <div class="stat-card">
      <div class="stat-title">AVG WAIT TIME</div>
      <div class="stat-value">4.5</div>
    </div>

    <div class="stat-card">
      <div class="stat-title">AVG TURNAROUND</div>
      <div class="stat-value">8.2</div>
    </div>

    <div class="stat-card">
      <div class="stat-title">PROCESSES</div>
      <div class="stat-value">12</div>
    </div>

  </div>

  <br>

  <!-- TABLE -->
  <div class="card">
    <div class="section-title">Scheduling Results</div>

    <table>
      <thead>
        <tr>
          <th>PID</th>
          <th>Type</th>
          <th>Priority</th>
          <th>Arrival</th>
          <th>Burst</th>
          <th>Wait</th>
          <th>TAT</th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>P1</td>
          <td>CPU</td>
          <td>1</td>
          <td>0</td>
          <td>5</td>
          <td>2</td>
          <td>7</td>
        </tr>

        <tr>
          <td>P2</td>
          <td>IO</td>
          <td>2</td>
          <td>1</td>
          <td>3</td>
          <td>1</td>
          <td>4</td>
        </tr>
      </tbody>
    </table>
  </div>

</main>

<footer>
SMART PROCESS SCHEDULER · MINI PROJECT 
</footer>

</body>
<footer style="
  position:relative;
  z-index:1;
  text-align:center;
  padding:24px;
  border-top:1px solid var(--border);
  margin-top:10px;
">
  <p style="
    font-size:9px;
    color:var(--text3);
    text-transform:uppercase;
    letter-spacing:3px;
    font-family:'Space Mono',monospace;
  ">
    Smart Process Scheduling System v2.1 &nbsp;·&nbsp;
    ML + OS + DBMS + DAA + Live Monitor &nbsp;·&nbsp;
    DHAYA R
  </p>
</footer>
</html>

